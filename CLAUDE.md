Fathom workspace notes:

- This branch now contains a Firecrawl-inspired landing page and interactive mock-test demo.
- The current generation flow is local and deterministic through `src/app/api/mock-exam/route.ts`.
- If you extend it, preserve the current visual language and avoid overwriting `src/components/landing/*` without checking the live page first.
